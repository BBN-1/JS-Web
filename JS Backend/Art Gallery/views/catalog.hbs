

            <section id="gallery">
                <h1>Gallery</h1>
                <article class="gallery-container">
                    {{#if allPublications}}
                    <!--If there are art publications in the database, show each of them-->
                    {{#each allPublications}}
                    <article class="card-info">
                        <article class="card-info-image">
                            <img src="{{artPicture}}" alt="art-image">
                        </article>
                        <article class="card-info-text">
                            <h2>Title: {{title}}</h2>
                            <h3>Certificate of authenticity: {{certificate}}</h3>
                            <a href="/catalog/{{_id}}" class="details-btn">Details</a>
                        </article>
                    </article>
                    {{/each}}


                    {{else}}
                    <article class="no-available-publications">
                        <h1>No publications created yet.</h1>
                        <a href="/create" class="create-pub">Create publication</a>
                    </article>
                    {{/if}}

                </article>
            </section>
